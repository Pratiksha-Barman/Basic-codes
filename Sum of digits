// Write a program to find the sum of digits of a 5 digit number.
#include <stdio.h>
void main()
{
int n, digit, temp, sum = 0;
scanf("%d",&n);
temp = n;
while(temp > 0)
{
    digit = temp % 10;
    sum = sum + digit;
    temp = temp / 10;
}
printf("%d\n",sum);
}
